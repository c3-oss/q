# Stub Compose file — uncomment services as you add dependencies (Postgres,
# Redis, MinIO, etc). The q service builds the local image from this
# repo's Dockerfile.
services:
  q:
    build:
      context: .
      target: q
    image: q:local
    # ports:
    #   - "8080:8080"
    # env_file:
    #   - .env
    # depends_on:
    #   - postgres

  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: q
  #     POSTGRES_PASSWORD: q
  #     POSTGRES_DB: q
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data

# volumes:
#   postgres-data:
